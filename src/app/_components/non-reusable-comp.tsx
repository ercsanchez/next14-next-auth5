// place components (used once) in this file's containing directory/folder
// _components will not be considered a route by next.js

export default function Component() {
  return <div>{`Non reusable Component (only used once)`}</div>;
}
